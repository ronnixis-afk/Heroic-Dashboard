# Supabase Configuration
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# RPG API Configuration
VITE_RPG_API_URL="http://localhost:3001"

# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY="pk_test_..."

# Administrative Access
# Comma-separated list of emails that should have admin access to the dashboard.
VITE_SUPER_ADMIN_EMAILS="admin@example.com"

# NOTE: VITE_ADMIN_API_KEY is no longer used for client-side authentication.
# The dashboard now uses JWT-based authentication via Clerk.
